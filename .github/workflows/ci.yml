# SPDX-License-Identifier: Apache-2.0
# Copyright (c) Viacheslav Shynkarenko

name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  test:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install the toolchain
        run: rustup toolchain install stable --profile minimal --component rustfmt,clippy

      - uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Test
        run: cargo test --workspace

      - name: Crate layering
        run: cargo xtask layering

      - name: Copyright headers
        run: cargo xtask headers

      - name: Workspace version
        run: cargo xtask version --check

      - name: Release binary size
        run: cargo xtask size

      # The installer runs on machines with nothing but a POSIX sh, so it is
      # linted as one. shellcheck is on the Ubuntu image, not the macOS one.
      - name: Installer
        if: runner.os == 'Linux'
        run: shellcheck -s sh install.sh
